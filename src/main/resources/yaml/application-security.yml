spring:
  config:
    import: optional:file:.env[.properties]
    activate:
      on-profile: security

  security:
    permit-all-paths:
      - /users/temporary/register
      - /users/email/verify-token
      - /users/finish-signup
      - /users/verify-duplicate/{account}
      - /users/validate-passwords-match
      - /users/find-account/{email}
      - /users/auth/send-code
      - /users/auth/verify-token
      - /users/reset-password
      - /users/email/resend-confirmation
      - /users/login
      - /users/refresh
      - /club-leader/login
      - /admin/login
      - /auth/refresh
      - /mainPhoto/**
      - /introPhoto/**
      - /notice/Photo/**
      - /my-notices/**
      - /clubs/**
      - /profiles/duplication-check
      - /users/existing/register
      - /mypages/clubs/{floor}/photo
      - /clubs/filter/**
      - /images/logo.png

    logging-paths:
      - /admin/clubs/category
      - /admin/clubs
      - /admin/clubs/{clubUUID}
      - /admin/floor/photo/{floor}
      - /notices/{noticeUUID}
      - /apply/{clubUUID}
      - /users/**
      - /profiles/**
      - /club-leader/**

    methods:
      - POST
      - DELETE
      - PUT
      - PATCH